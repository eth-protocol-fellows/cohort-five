"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.RightPadCalculator = void 0;
var basePadCalculator_1 = require("../basePadCalculator");
var RightPadCalculator = /** @class */ (function (_super) {
    __extends(RightPadCalculator, _super);
    function RightPadCalculator() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    RightPadCalculator.prototype.getLeftPadding = function (paddingChar, table, row, column) {
        return paddingChar.repeat(this.getLeftPaddingCount(table, row, column));
    };
    RightPadCalculator.prototype.getRightPadding = function (paddingChar, table, row, column) {
        return paddingChar;
    };
    RightPadCalculator.prototype.getLeftPaddingCount = function (table, row, column) {
        var longestColumnLength = table.getLongestColumnLengths()[column];
        var leftPadCount = longestColumnLength > 0
            ? longestColumnLength - table.rows[row].cells[column].getLength()
            : 1;
        leftPadCount += this.extraPadCount(table);
        return leftPadCount;
    };
    RightPadCalculator.prototype.extraPadCount = function (table) {
        return 1;
    };
    ;
    return RightPadCalculator;
}(basePadCalculator_1.BasePadCalculator));
exports.RightPadCalculator = RightPadCalculator;
